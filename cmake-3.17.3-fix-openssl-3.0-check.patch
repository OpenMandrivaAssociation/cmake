diff -up cmake-3.17.3/Modules/FindOpenSSL.cmake.omv~ cmake-3.17.3/Modules/FindOpenSSL.cmake
--- cmake-3.17.3/Modules/FindOpenSSL.cmake.omv~	2020-06-05 14:39:10.821555035 +0200
+++ cmake-3.17.3/Modules/FindOpenSSL.cmake	2020-06-05 14:39:49.893964847 +0200
@@ -419,8 +419,8 @@ if(OPENSSL_INCLUDE_DIR AND EXISTS "${OPE
     # Since OpenSSL 3.0.0, the new version format is MAJOR.MINOR.PATCH and
     # a new OPENSSL_VERSION_STR macro contains exactly that
     file(STRINGS "${OPENSSL_INCLUDE_DIR}/openssl/opensslv.h" OPENSSL_VERSION_STR
-         REGEX "^#[\t ]*define[\t ]+OPENSSL_VERSION_STR[\t ]+\"([0-9])+\.([0-9])+\.([0-9])+\".*")
-    string(REGEX REPLACE "^.*OPENSSL_VERSION_STR[\t ]+\"([0-9]+\.[0-9]+\.[0-9]+)\".*$"
+         REGEX "^#[\t ]*define[\t ]+OPENSSL_VERSION_STR[\t ]+\"([0-9])+\\.([0-9])+\\.([0-9])+\".*")
+    string(REGEX REPLACE "^.*OPENSSL_VERSION_STR[\t ]+\"([0-9]+\\.[0-9]+\\.[0-9]+)\".*$"
            "\\1" OPENSSL_VERSION_STR "${OPENSSL_VERSION_STR}")
 
     set(OPENSSL_VERSION "${OPENSSL_VERSION_STR}")
